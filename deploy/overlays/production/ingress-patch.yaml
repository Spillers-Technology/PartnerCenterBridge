apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: pcbridge
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts: ["pcbridge.example.com"]
      secretName: pcbridge-tls
  rules:
    - host: pcbridge.example.com
