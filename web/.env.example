# Leave OIDC vars blank for local dev against an API running with Auth:Enabled=false.
# Populate them to authenticate operators through Authentik in a deployed environment.
VITE_OIDC_AUTHORITY=
VITE_OIDC_CLIENT_ID=partnercenterbridge
VITE_API_BASE=
